---
paths:
  - '**/*.php'
---

# PHP

## Prefer class properties over constants, protected over private
Where possible, use class properties rather than class constants.

Default to `protected` for methods and properties; only use `private` where there is a good reason.
