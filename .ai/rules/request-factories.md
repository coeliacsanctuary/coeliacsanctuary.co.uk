---
paths:
  - 'tests/RequestFactories/**'
---

# Request Factories

## Build request payloads with request factories
Request payloads for tests are built with worksome/request-factories classes in `tests/RequestFactories/<Domain>/`, named `*RequestFactory`.

Add or reuse a factory rather than hand-rolling payload arrays inline in the test.
